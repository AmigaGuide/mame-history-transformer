This folder contains:

- Raw XML datasets used for parsing (excluded from Git)
- JSON files generated during development and testing (also excluded)

These files are excluded from version control via `.gitignore` to avoid bloat and licensing issues.
